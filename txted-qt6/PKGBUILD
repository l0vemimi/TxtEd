# Maintainer: Your Name <your.email@example.com>
pkgname=txted
pkgver=1.0.0
pkgrel=1
pkgdesc="Very simple python text editor with qt6 & wayland compatability."
arch=('any')
url="https://github.com/l0vemimi/txted"
license=('GPL')
depends=('python' 'pyqt6' 'pygments')
makedepends=('git')
source=("git+https://github.com/l0vemimi/txted.git#tag=v$pkgver"
        "txted-qt6.desktop"
        "txt-qt.png")
md5sums=('SKIP' 'SKIP' 'SKIP')

package() {
    cd "$srcdir/txted"

    python setup.py install --root="$pkgdir/" --optimize=1

    install -Dm644 "$srcdir/txted-qt6.desktop" "$pkgdir/usr/share/applications/txted-qt6.desktop"

    install -Dm644 "$srcdir/txt-qt.png" "$pkgdir/usr/share/icons/hicolor/256x256/apps/txted.png"
}
